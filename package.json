{
  "name": "veil-chat-root",
  "private": true,
  "workspaces": [
    "src/server",
    "src/client"
  ],
  "scripts": {
    "start:websocket": "npm run start:websocket --workspace=src/server",
    "start:express": "npm run start:express --workspace=src/server",
    "build:client": "npm run build --workspace=src/client"
  }
}